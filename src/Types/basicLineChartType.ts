export type BasicLineChart = {
  ActiveUsers: readonly (number | null)[];
  Mounth: readonly (number | null)[];
  NewUers: readonly (number | null)[];
};
export type salesChartDataType = {
  Income: number;
  Visitors: number;
  CompletedOrders: number;
  PendingOrders: number;
  month: string;
};
